<h2><%= @list.name %></h2>
<ul>
<% @list.bookmarks.each do |bookmark| %>
<li><%= bookmark.comment %> <%= bookmark.movie.title %> <img src="<%= bookmark.movie.poster_url %>" alt="poster_url" width=300px> <%= link_to "Delete", bookmark_path(bookmark),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %> </li>
<% end %>
</ul>

<%= link_to "New bookmark", new_list_bookmark_path(@list) %>

<%= link_to "Back", lists_path %>
